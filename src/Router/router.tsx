import { Route, Routes } from "react-router-dom";

const Router = () => {
  return (
    <Routes>
        {/* <Route index element={<HomePage />} /> */}
      {/* <Route path="/" element={<Layout />}>
        <Route index element={<HomePage />} />
      </Route> */}
    </Routes>
  );
};
